export const metadata = {
  title: "Security & Screen Lock — DriveSync",
  description: "Configure idle and background screen lock timeouts.",
};

export default function SecuritySettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
